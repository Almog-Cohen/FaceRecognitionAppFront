import React from 'react';
import RankCard from './RankCard';


const RankList = () => {

    return (
        <div >
            {
                // robots.map((user, i) => {
                //     return (
                //         <RankCard 
                //             // key={i}
                //             // id={robots[i].id}
                //             // name={robots[i].name}
                //             // email={robots[i].email}
                //         />
                //     );
                // })
            }
        </div>
    );
}

export default RankList;